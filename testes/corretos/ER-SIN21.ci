@cep(9, "Falta ';' apos a ultima atribuicao", "';' expected")

class Program
    func run {

        var Int i;
        i = 1;
        while i <= 5 {
            i = i + 1
        }
    }
end
