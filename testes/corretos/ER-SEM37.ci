/*
Evelin Soares
evelincontatos@gmail.com
*/

@cep(17, "Metodo 'set' nao pertence a classe 'A'", "Method 'set' was not found in class 'A' or its superclasses", "comp.Compiler.factor()")


class A

    func m {

    }

end

class Program

    func run {
        var A a;

        a = A.new;
        a.set: 0;
    }

end
