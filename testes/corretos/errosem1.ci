/*
Vitor Mesquita Fogaca
vitor.fogaca@outlook.com
*/

class Teste
    var Int n;

    func get -> Int {
        return self.n;
    }

    func set: Int n {
        self.n = n;
    }
end

@cep(8, "variavel erro nao foi declarada", "variable 'erro' not found")

class Program
    func run {
        var Teste t;
        var Int a;
        t = Teste.new;
        a = In.readInt;
        t.set: erro;
        Out.println: t.get;
    }
end
