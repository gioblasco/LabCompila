/*
Vitor Mesquita Fogaca
vitor.fogaca@outlook.com
*/

@cep(3, "var sem tipo", "missing type in variable declaration")

class Teste
    var n;

    func get -> Int {
        return self.n;
    }

    func set: Int n {
        self.n = n;
    }
end

class Program
    func run {
        var Teste t;
        var Teste a;
        t = Teste.new;
        a = In.readInt;
        t.set: a;
        Out.println: t.get;
    }
end
