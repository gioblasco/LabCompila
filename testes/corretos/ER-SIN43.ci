/*
    Lucca Carvalho
    luccalafonte97@gmail.com
*/

@cep(5, "nao existe a declaracao de packages",
   "function or field declaration expected", "comp.Compiler.classDec()")

class Program

    package m;

    func run {
    }

end
