/*
Vitor Mesquita Fogaca
vitor.fogaca@outlook.com
*/
      
@cep(5, "Falta de 'return'", "missing 'return' statement")

class Teste
    var Int n;
    
    func get -> Int { }
    
    func set: Int n {
        self.n = n;
    }
end

class Program
    func run {
        var Teste t;
        var Int a;
        t = Teste.new;
        a = In.readInt;
        t.set: a;
        Out.println: t.get;
    }
end
