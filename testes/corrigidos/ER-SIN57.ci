/*
    Lucca Carvalho
    luccalafonte97@gmail.com
*/

@cep(6, "Palavras chave nao podem estar em maiuscula", 
  "'public', 'private' or '}' expected",
  "comp.Compiler.classDec()")

class Program

    PUBLIC func m {
    }

    func run {

        var A a;
        a = A.new;
        a.m;
    }

end
