@cep(9, "Metodo sobrescrevido sem override",  "An overridden method should be preceded by 'override'")

open 
class A
    public func nothing {
    }
end

class B extends A
    public func nothing {
    }
end

class Program 
    func run {
    }
end
