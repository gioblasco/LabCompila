/*
    Lucca Carvalho
    luccalafonte97@gmail.com
*/

@cep(9, "Construtores nao tomam parametros", 
  "')' expected",
  "comp.Compiler.factor()")

class Program

    func run {

        var Program p;
        p = Program.new: nil;
    }

end
