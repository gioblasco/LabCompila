/*
    Caroline Aparecida
    hicaarol@gmail.com
*/

@cep(7, "Metodo que nao deve retornar nada possui instrucao 'return'",
"Illegal 'return' statement. Method returns 'void'",
"comp.Compiler.returnStatement()")

class A

    func m {
        return 0;
    }

end

class Program

    func run {
        var A a;
        a = A.new;
        a.m;
    }

end
